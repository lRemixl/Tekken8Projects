local _mod_config = {
    -- Kazuya --
    switchElectricHitEffectKazuyaColor = true,           -- Set to true to use the below
    ElectricHitEffectKazuyaColor = {218,165,32},           -- Requires above to be true : (R,G,B) Values for the electric color (0-255)
    ElectricHitEffectKazuyaColorIntensityBoost = 1.0,     -- (Brightness intensity) 1.0 is normal, values below 1.0 decrease its intensity values above 1.0 increase it , (NOT THE BEST TO USE)
    ElectricHitEffectKazuyaColorSaturationBooster = 1.0,   -- (Color intensity) 1.0 is normal, values below 1.0 decreases saturation, values above 1.0 increase it
    -- Devil Jin -- 
    switchElectricHitEffectDevilJinColor = false,           -- Set to true to use the below
    ElectricHitEffectDevilJinColor = { 0, 0, 0 },           -- Requires above to be true : (R,G,B) Values for the electric color (0-255)
    ElectricHitEffectDevilJinColorIntensityBoost = 1.0,     -- (Brightness intensity) 1.0 is normal, values below 1.0 decrease its intensity values above 1.0 increase it , (NOT THE BEST TO USE)
    ElectricHitEffectDevilJinColorrSaturationBooster = 1.0,   -- (Color intensity) 1.0 is normal, values below 1.0 decreases saturation, values above 1.0 increase it
    -- Jin -- 
    switchElectricHitEffectJinColor = false,           -- Set to true to use the below
    ElectricHitEffectJinColor = { 0, 0, 0 },           -- Requires above to be true : (R,G,B) Values for the electric color (0-255)
    ElectricHitEffectJinColorIntensityBoost = 1.0,     -- (Brightness intensity) 1.0 is normal, values below 1.0 decrease its intensity values above 1.0 increase it , (NOT THE BEST TO USE)
    ElectricHitEffectJinColorSaturationBooster = 1.0,   -- (Color intensity) 1.0 is normal, values below 1.0 decreases saturation, values above 1.0 increase it
    -- Reina -- 
    switchElectricHitEffectReinaColor = false,           -- Set to true to use the below
    ElectricHitEffectReinaColor = { 0, 0, 0 },           -- Requires above to be true : (R,G,B) Values for the electric color (0-255)
    ElectricHitEffectReinaColorIntensityBoost = 1.0,     -- (Brightness intensity) 1.0 is normal, values below 1.0 decrease its intensity values above 1.0 increase it , (NOT THE BEST TO USE)
    ElectricHitEffectReinaColorSaturationBooster = 1.0,   -- (Color intensity) 1.0 is normal, values below 1.0 decreases saturation, values above 1.0 increase it
}

return _mod_config